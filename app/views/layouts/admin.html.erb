<!DOCTYPE html>
<html>
  <head>
    <title>Admin · Clinic Booking</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>
  <body class="bg-slate-100 min-h-screen text-slate-800">
    <div class="flex min-h-screen">
      <aside class="w-64 bg-slate-900 text-white hidden lg:flex flex-col">
        <div class="px-6 py-6 border-b border-slate-800">
          <span class="text-xl font-semibold tracking-tight">Clinic Admin</span>
        </div>
        <nav class="flex-1 px-4 py-6 space-y-1">
          <%= admin_nav_link "Dashboard", admin_root_path, icon: "chart-bar" %>
          <%= admin_nav_link "Doctors", admin_doctors_path, icon: "user-group" %>
          <%= admin_nav_link "Calendar", admin_calendar_path, icon: "calendar" %>
          <%= admin_nav_link "Appointments", admin_appointments_path, icon: "clipboard" %>
          <%= admin_nav_link "Time Slots", admin_time_slots_path, icon: "clock" %>
        </nav>
      </aside>

      <div class="flex-1 flex flex-col">
        <header class="bg-white border-b border-slate-200">
          <div class="flex items-center justify-between px-4 sm:px-6 lg:px-8 h-16">
            <div class="lg:hidden">
              <span class="text-lg font-semibold">Clinic Admin</span>
            </div>
            <div class="flex items-center space-x-4 text-sm text-slate-600">
              <% if current_user %>
                <span class="font-medium text-slate-700">Signed in as <%= current_user.email %></span>
                <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "px-3 py-2 rounded-md bg-slate-900 text-white hover:bg-slate-700" %>
              <% end %>
            </div>
          </div>
        </header>

        <main class="flex-1 px-4 sm:px-6 lg:px-8 py-8">
          <%= render "shared/toasts" %>
          <%= yield %>
        </main>
      </div>
    </div>
    <%= yield :scripts %>
  </body>
</html>
