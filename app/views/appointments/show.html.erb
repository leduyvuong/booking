<div class="space-y-8">
  <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
    <div class="flex flex-col gap-4 md:flex-row md:items-start md:justify-between">
      <div>
        <p class="text-xs font-semibold uppercase tracking-wide text-blue-600">Booking confirmed</p>
        <h1 class="mt-2 text-3xl font-semibold text-slate-900">Appointment <span class="font-mono text-base text-blue-600"><%= @appointment.booking_number %></span></h1>
        <p class="mt-2 text-sm text-slate-500">We’ve sent a confirmation email to <strong class="font-semibold text-slate-700"><%= @appointment.patient_email %></strong>.</p>
      </div>
      <div class="flex items-center gap-2">
        <% status_class = case @appointment.status
                         when "confirmed" then "bg-emerald-100 text-emerald-700"
                         when "pending" then "bg-amber-100 text-amber-700"
                         else "bg-rose-100 text-rose-700"
                         end %>
        <span class="inline-flex items-center rounded-full px-4 py-1 text-xs font-semibold <%= status_class %>">
          <%= @appointment.status.humanize %>
        </span>
      </div>
    </div>
  </div>

  <div class="grid gap-6 lg:grid-cols-3">
    <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2">
      <div class="grid gap-6 md:grid-cols-2">
        <div>
          <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-400">Doctor</h2>
          <p class="mt-2 text-lg font-semibold text-slate-900"><%= @appointment.time_slot.doctor.name %></p>
          <p class="text-sm font-medium text-blue-600"><%= @appointment.time_slot.doctor.specialty %></p>
          <p class="mt-2 text-sm text-slate-600"><%= @appointment.time_slot.doctor.clinic.name %></p>
        </div>
        <div>
          <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-400">When</h2>
          <p class="mt-2 text-lg font-semibold text-slate-900"><%= @appointment.time_slot.date.strftime('%A, %B %-d') %></p>
          <p class="text-sm text-slate-600"><%= @appointment.time_slot.start_time.strftime('%-I:%M %p') %> – <%= @appointment.time_slot.end_time.strftime('%-I:%M %p') %></p>
        </div>
        <div>
          <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-400">Patient</h2>
          <p class="mt-2 text-lg font-semibold text-slate-900"><%= @appointment.patient_name %></p>
          <p class="text-sm text-slate-600"><%= @appointment.patient_phone %></p>
          <p class="text-sm text-slate-600"><%= @appointment.patient_email %></p>
        </div>
        <% if @appointment.notes.present? %>
          <div>
            <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-400">Reason for visit</h2>
            <p class="mt-2 whitespace-pre-line text-sm text-slate-600"><%= @appointment.notes %></p>
          </div>
        <% end %>
      </div>
    </div>

    <div class="space-y-4">
      <div class="rounded-3xl border border-blue-200 bg-blue-50 p-5 text-sm text-blue-800">
        <p class="font-semibold">Arrive 10 minutes early</p>
        <p class="mt-1">Bring a photo ID and insurance card if applicable.</p>
      </div>
      <div class="rounded-3xl border border-slate-200 bg-white p-5 text-sm text-slate-600">
        <h2 class="text-sm font-semibold uppercase tracking-wide text-slate-400">Next steps</h2>
        <ul class="mt-3 space-y-2">
          <li class="flex items-start gap-2">
            <span class="mt-1 h-2 w-2 flex-shrink-0 rounded-full bg-blue-500"></span>
            Add this appointment to your calendar.
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-1 h-2 w-2 flex-shrink-0 rounded-full bg-blue-500"></span>
            Reschedule or cancel from your dashboard if plans change.
          </li>
        </ul>
        <div class="mt-4 flex flex-col gap-2">
          <%= link_to "View my appointments", my_appointments_path, class: "inline-flex items-center justify-center rounded-full bg-blue-600 px-4 py-2 text-sm font-semibold text-white shadow hover:bg-blue-700" if user_signed_in? %>
          <%= link_to "Book another visit", doctors_path, class: "inline-flex items-center justify-center rounded-full border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-600 hover:border-blue-200 hover:text-blue-600" %>
        </div>
      </div>
    </div>
  </div>
</div>
