<div class="max-w-3xl">
  <h1 class="text-2xl font-semibold text-slate-900">New time slot</h1>
  <p class="mt-1 text-sm text-slate-500">Create a single availability window for a doctor.</p>

  <div class="mt-8 rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form" %>
  </div>
</div>
